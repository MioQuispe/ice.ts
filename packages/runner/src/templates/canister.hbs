import { ActorSubclass } from "@dfinity/agent"
import type { _SERVICE } from "{{canister_service_path}}"
export { idlFactory } from "{{canister_idl_factory_path}}"
export const canisterId = {{{canister_id}}};
export const service = {} as ActorSubclass<{{{'_SERVICE'}}}>;
export const actor = {} as ActorSubclass<{{{'_SERVICE'}}}>;