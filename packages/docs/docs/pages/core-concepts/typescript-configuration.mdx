# TypeScript-Based Configuration

ICE replaces the traditional `dfx.json` file with a more ergonomic and type-safe `ice.config.ts` file.

## Basic Structure

A typical `ice.config.ts` file looks like this: