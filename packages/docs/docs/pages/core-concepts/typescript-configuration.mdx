# TypeScript-Based Configuration

Crystal replaces the traditional `dfx.json` file with a more ergonomic and type-safe `crystal.config.ts` file.

## Basic Structure

A typical `crystal.config.ts` file looks like this: